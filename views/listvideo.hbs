
        {{#if error}}
      error
        {{else}}

{
  "data": {
    "videos": [ 
                    {{#each meta.formats}}

       {
        "url": "{{url}}",
        "format": "{{format_note}}",
        "size": "{{filesize}}"
      },  



                    {{/each}}
  ]
  }
}
        {{/if}}

